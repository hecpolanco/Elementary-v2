<!DOCTYPE html>
<html>
  <head>
    <title>Elementary</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body style="background-color: #0000CD " class="text-white" font-family="IBM Plex Mono", monospace;>
  <nav class="navbar navbar-expand-lg navbar-light bg-dark">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
    <% if user_signed_in? %>
      <%= link_to "My Watsons", watsons_path, :class => "btn btn-light" %>
      <%=link_to "Analyze Text", new_watson_path, :class => "btn btn-primary" %>
      <%= link_to 'Profile', edit_user_registration_path, :class => 'navbar-link btn btn-light' %> 
      <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link btn btn-light'  %>
    <% end %>
      </li>
    </ul>
  </div>
</nav>
    <%= yield %>
  </body>
</html>
